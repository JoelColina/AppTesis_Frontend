import { SubtitlePipePipe } from './subtitle.pipe.pipe';

describe('SubtitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SubtitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
